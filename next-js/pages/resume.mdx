import { Button } from 'theme-ui';
import { Meta } from '../components/Meta';
import { ResumeHtml } from '../components/ResumeHtml';
import resumeJson from '../../resume/resume.json';

export const DownloadAsPDF = () => {
  return (
    <div className="no-print">
      <Button
        as="a"
        href="/assets/Andrew Westling Resume.pdf"
        variant="small"
        target="_blank"
        sx={{ marginY: 4 }}
      >
        📃 Download as PDF
      </Button>
    </div>
  );
};

<Meta title={'Resume'} date={resumeJson.meta.lastModified} />

<DownloadAsPDF />

<ResumeHtml />
